수강신청프로그램

학생테이블 : 1명의 학생은 여러 과정을 수강신청이 가능(1:N)
과정테이블 : 해당과목은 여러학생이 수강신청이 가능(1:N)
수강신청테이블 : 수강한 학생, 수강신청한 과정
N:1               N:1
학생테이블(1)(부모)-----<수강신청테이블(N)(자식)
과정테이블(1)(부모)-----<수강신청테이블(N)(자식)

### 📘 학생 테이블 (StudentEntity)
| 필드명           | 타입                     | 설명                   |
| ------------- | ---------------------- | -------------------- |
| `id`          | Long                   | 학생 고유 번호 (PK, 자동 생성) |
| `name`        | String                 | 학생 이름                |
| `email`       | String                 | 학생 이메일 (고유, 필수)      |
| `enrollments` | List<EnrollmentEntity> | 학생의 수강신청 목록 (1:N 관계) |

### 📘 과정 테이블 (CourseEntity)
| 필드명           | 타입                     | 설명                         |
| ------------- | ---------------------- | -------------------------- |
| `id`          | Long                   | 과목 고유 번호 (PK, 자동 생성)       |
| `title`       | String                 | 과목명 (필수)                   |
| `credit`      | int                    | 학점 (필수)                    |
| `professor`   | String                 | 담당 교수명 (필수)                |
| `enrollments` | List<EnrollmentEntity> | 해당 과목을 수강하는 학생 목록 (1:N 관계) |

### 📘 수강신청 테이블 (EnrollmentEntity)
| 필드명          | 타입            | 설명                          |
| ------------ | ------------- | --------------------------- |
| `id`         | Long          | 수강신청 고유 번호 (PK, 자동 생성)      |
| `student`    | StudentEntity | 수강신청 학생 (N:1 관계)            |
| `course`     | CourseEntity  | 수강신청 과목 (N:1 관계)            |
| `enrollDate` | LocalDate     | 수강신청일 (필수)                  |
| `score`      | Integer       | 성적 (nullable, 채점 전 null 가능) |


### 📘 API 정의서 
| 기능            | Method | 경로                      | 설명             | 요청 파라미터 / 요청 바디                     | 반환 / 처리                                |
| ------------- | ------ | ----------------------- | -------------- | ----------------------------------- | -------------------------------------- |
| **시작 페이지**    | GET    | `/`                     | 메인 페이지로 이동     | 없음                                  | `index.html` 렌더링                       |
| **학생 전체 조회**  | GET    | `/students`             | 학생 목록 페이지      | 없음                                  | `student/list.html` + 학생 리스트 모델        |
| **학생 등록 페이지** | GET    | `/students/new`         | 학생 등록 폼 페이지    | 없음                                  | `student/create.html`                  |
| **학생 등록 처리**  | POST   | `/students`             | 학생 생성          | StudentRequest                      | DB 저장 후 `/students` 리다이렉트              |
| **학생 수정 페이지** | GET    | `/students/edit/{id}`   | 특정 학생 수정 폼     | `id` (PathVariable)                 | `student/edit.html` + 학생 데이터 모델        |
| **학생 수정 처리**  | POST   | `/students/edit/{id}`   | 특정 학생 정보 업데이트  | `id` (PathVariable), StudentRequest | DB 업데이트 후 `/students` 리다이렉트            |
| **학생 삭제**     | GET    | `/students/delete/{id}` | 특정 학생 삭제       | `id` (PathVariable)                 | DB 삭제 후 `/students` 리다이렉트              |
| **학생 상세 페이지** | GET    | `/students/{id}`        | 특정 학생 상세 정보 조회 | `id` (PathVariable)                 | `student/view.html` + 학생 데이터 모델        |
| **강의 목록**     | GET    | `/courses`              | 강의 목록 페이지      | 없음                                  | `course/list.html` + 강의 리스트 모델         |
| **강의 등록 폼**   | GET    | `/courses/new`          | 강의 등록 폼 페이지    | 없음                                  | `course/create.html`                   |
| **강의 등록 처리**  | POST   | `/courses`              | 강의 생성          | CourseRequest                       | DB 저장 후 `/courses` 리다이렉트               |
| **강의 수정 폼**   | GET    | `/courses/edit/{id}`    | 특정 강의 수정 폼     | `id` (PathVariable)                 | `course/edit.html` + 강의 데이터 모델         |
| **강의 수정 처리**  | POST   | `/courses/edit/{id}`    | 특정 강의 정보 업데이트  | `id` (PathVariable), CourseRequest  | DB 업데이트 후 `/courses` 리다이렉트             |
| **강의 삭제**     | GET    | `/courses/delete/{id}`  | 특정 강의 삭제       | `id` (PathVariable)                 | DB 삭제 후 `/courses` 리다이렉트               |
| **강의 상세 페이지** | GET    | `/courses/{id}`         | 특정 강의 상세 정보 조회 | `id` (PathVariable)                 | `course/view.html` + 강의 데이터 모델         |
| **수강신청 폼**    | GET    | `/enrollments/new`      | 수강신청 페이지       | 없음                                  | `enrollment/create.html` + 학생/강의 목록 모델 |
| **수강신청 처리**   | POST   | `/enrollments`          | 학생이 강의를 신청     | EnrollmentRequest                   | DB 저장 후 `/enrollments/list` 리다이렉트      |
| **수강신청 목록**   | GET    | `/enrollments/list`     | 전체 학생 수강목록 확인  | 없음                                  |                                        |


### 📘 학생 서비스 로직 정의서 (StudentService)
| 함수 이름            | 매개변수                              | 반환형                     | 설명                | 특징 / 비고           |
| ---------------- | --------------------------------- | ----------------------- | ----------------- | ----------------- |
| `createStudent`  | `StudentRequest request`          | `StudentResponse`       | 신규 학생 등록          | 이메일 중복 체크 포함      |
| `getStudent`     | `Long id`                         | `StudentResponse`       | 단일 학생 조회 후 DTO 변환 | 수강과목 리스트, 수강 수 포함 |
| `getAllStudents` | 없음                                | `List<StudentResponse>` | 전체 학생 조회 후 DTO 변환 | 수강과목 리스트, 수강 수 포함 |
| `updateStudent`  | `Long id, StudentRequest request` | `StudentResponse`       | 기존 학생 정보 수정       | 존재하지 않으면 예외 발생    |
| `deleteStudent`  | `Long id`                         | `boolean`               | 학생 삭제             | 존재하지 않으면 false 반환 |
- StudentService → 학생 관련 CRUD + 수강 내역 조회

### 📘 강의 서비스 로직 정의서 (CourseService)
| 함수 이름           | 매개변수                             | 반환형                    | 설명                | 특징 / 비고              |
| --------------- | -------------------------------- | ---------------------- | ----------------- | -------------------- |
| `createCourse`  | `CourseRequest request`          | `CourseResponse`       | 신규 강의 등록          |                      |
| `getCourse`     | `Long id`                        | `CourseResponse`       | 단일 강의 조회 후 DTO 변환 | 수강학생 이름 리스트, 수강 수 포함 |
| `getAllCourses` | 없음                               | `List<CourseResponse>` | 전체 강의 조회 후 DTO 변환 | 수강학생 이름 리스트, 수강 수 포함 |
| `updateCourse`  | `Long id, CourseRequest request` | `CourseResponse`       | 기존 강의 정보 수정       | 존재하지 않으면 예외 발생       |
| `deleteCourse`  | `Long id`                        | `boolean`              | 강의 삭제             | 존재하지 않으면 false 반환    |
- CourseService → 강의 관련 CRUD + 수강 학생 조회

### 📘 수강 서비스 로직 정의서 (EnrollmentService)
| 함수 이름                     | 매개변수                               | 반환형                        | 설명              | 특징 / 비고           |
| ------------------------- | ---------------------------------- | -------------------------- | --------------- | ----------------- |
| `enroll`                  | `EnrollmentRequest request`        | `EnrollmentResponse`       | 학생의 수강 신청       | 이미 신청한 과목이면 예외 발생 |
| `getEnrollment`           | `Long id`                          | `EnrollmentResponse`       | 단일 수강 정보 조회     |                   |
| `getEnrollmentsByStudent` | `Long studentId`                   | `List<EnrollmentResponse>` | 특정 학생의 수강 내역 조회 |                   |
| `updateScore`             | `Long enrollmentId, Integer score` | `EnrollmentResponse`       | 수강 성적 입력/수정     |                   |
| `cancelEnrollment`        | `Long enrollmentId`                | `boolean`                  | 수강 취소           | 존재하지 않으면 false 반환 |
- EnrollmentService → 수강 신청, 조회, 취소, 성적 입력